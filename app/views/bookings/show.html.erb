
<h1> You have booked <%= @booking.seats %> seats </h1>

<%= link_to 'Edit', edit_booking_path(@booking) %>
<%= link_to 'Delete', booking_path(@booking), method: :delete, data: { confirm: 'Sind Sie sicher?'} %>
<%= link_to 'Zurück', new_booking_path %>
