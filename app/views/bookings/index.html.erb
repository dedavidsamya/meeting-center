
<div class="container container-custom">
  <div class="row row-custom">
    <div class="col-6 centering form rounded-input mt-3 p-4 shadow mb-3">
      <h1>Booking requests overview</h1>
        <%= @bookings.each do |booking| %>
        <div class="card mt-3 p-4 shadow mb-3">
          <p>Name: <%= booking.name %></p>
          <p>E-mail: <%= booking.email %></p>
          <p>Meeting room: <%= booking.sunset && booking.sunrise ? 'Sunset and Sunrise' : booking.sunrise ? 'Sunrise' : booking.sunset ? 'Sunset' : nil %></p>
          <p>Start date: <%= booking.beginning %></p>
          <p>End date: <%= booking.end %></p>
          <p>Number of seats: <%= booking.seats %></p>
          <p>Catering? <%= booking.catering ? 'Yes' : 'No'  %></p>
        </div> -->
        <% end %>
    </div>
  </div>
</div>

