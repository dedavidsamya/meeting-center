<h1>Here I want to show only the selected booking</h1>
<h1> You have booked <%= @booking.seats %> seats </h1>

<%= link_to 'Edit', edit_booking_path(@booking) %>
<%= link_to 'Delete', booking_path(@booking), method: :delete, data: { confirm: 'Are you sure?'} %>
<%= link_to 'Back to booking page', new_booking_path %>
